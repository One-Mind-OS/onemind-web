services:
  onemind:
    image: ghcr.io/one-mind-os/onemind-web:latest
    build: .
    ports:
      - "3456:3456"
      - "3457:3457"
    volumes:
      - ./data:/app/data
      - ./.env.local:/app/.env.local
    restart: unless-stopped
