services:
  onemind:
    image: ghcr.io/one-mind-os/onemind-web:latest
    build: .
    ports:
      - "3456:3456"
      - "3457:3457"
    env_file:
      - .env.local
    volumes:
      - ./data:/app/data
    restart: unless-stopped
